const initialState = {
  characters: [],
  characterDetail: {},
};

export const characterReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
