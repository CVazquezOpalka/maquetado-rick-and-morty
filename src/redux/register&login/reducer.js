const initialState = {
  userName: "",
  userToken: "",
  userEmail: "",
};

export const LoginAndRegisterReducer = (
  state = initialState,
  { type, payload }
) => {
  switch (type) {
    default:
      return state;
  }
};
