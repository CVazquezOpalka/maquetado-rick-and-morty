const initialState = {
  previousPage: "",
  nextPage: "",
  actualPage: "",
};

export const PaginationReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
